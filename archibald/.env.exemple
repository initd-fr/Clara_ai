# Utiliser le fichier .env à la racine du dépôt.
# Copier : cp ../.env.exemple ../.env
# Puis remplir ../.env et lancer ../start.sh (Docker) ou depuis la racine : uv run --directory archibald uvicorn app.main:app --reload
# Archibald charge automatiquement le .env racine (voir app/core/config.py).
