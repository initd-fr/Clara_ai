# Utiliser le fichier .env à la racine du dépôt.
# Copier : cp ../.env.exemple ../.env
# Puis remplir ../.env et lancer ../start.sh (Docker) ou pnpm dev (local).
# Clara charge automatiquement le .env racine (voir next.config.js).
